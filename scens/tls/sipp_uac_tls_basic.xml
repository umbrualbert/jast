<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  SIPp UAC Scenario - TLS/SIPS Basic Call

  This scenario demonstrates a basic SIP call over TLS (SIPS).

  Usage:
    sipp -t l1 -tls_cert /certs/sipp.crt -tls_key /certs/sipp.key \
         -sf /scens/tls/sipp_uac_tls_basic.xml \
         -r 10 -m 100 <sbc_ip>:5061

  Features:
    - TLS transport (SIPS)
    - Basic INVITE/200/ACK/BYE flow
    - Secure signaling

  Note: SBC must support TLS on port 5061
-->

<scenario name="UAC TLS Basic Call">

  <!-- Send INVITE over TLS -->
  <send retrans="500">
    <![CDATA[

      INVITE sips:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/TLS [local_ip]:[local_port];branch=[branch]
      From: <sips:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: <sips:[service]@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: <sips:sipp@[local_ip]:[local_port]>
      Max-Forwards: 70
      Subject: TLS Performance Test
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=user1 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=TLS SIPp Call
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0 8 101
      a=rtpmap:0 PCMU/8000
      a=rtpmap:8 PCMA/8000
      a=rtpmap:101 telephone-event/8000
      a=fmtp:101 0-15
      a=ptime:20
      a=sendrecv

    ]]>
  </send>

  <!-- Receive 100 Trying (optional) -->
  <recv response="100" optional="true">
  </recv>

  <!-- Receive 180 Ringing (optional) -->
  <recv response="180" optional="true">
  </recv>

  <!-- Receive 183 Session Progress (optional) -->
  <recv response="183" optional="true">
  </recv>

  <!-- Receive 200 OK for INVITE -->
  <recv response="200" rtd="true">
  </recv>

  <!-- Send ACK -->
  <send>
    <![CDATA[

      ACK sips:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/TLS [local_ip]:[local_port];branch=[branch]
      From: <sips:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: <sips:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Contact: <sips:sipp@[local_ip]:[local_port]>
      Max-Forwards: 70
      Content-Length: 0

    ]]>
  </send>

  <!-- Hold the call for some time (5 seconds) -->
  <pause milliseconds="5000"/>

  <!-- Send BYE -->
  <send retrans="500">
    <![CDATA[

      BYE sips:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/TLS [local_ip]:[local_port];branch=[branch]
      From: <sips:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: <sips:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 2 BYE
      Contact: <sips:sipp@[local_ip]:[local_port]>
      Max-Forwards: 70
      Content-Length: 0

    ]]>
  </send>

  <!-- Receive 200 OK for BYE -->
  <recv response="200" crlf="true">
  </recv>

  <!-- Statistics -->
  <ResponseTimeRepartition value="10, 20, 30, 40, 50, 100, 150, 200"/>
  <CallLengthRepartition value="10, 50, 100, 500, 1000, 5000, 10000"/>

</scenario>
